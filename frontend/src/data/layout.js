// Shared layout geometry, kept in one place so the desktop grid, the dock
// clearance, and window centering/maximize math never drift out of sync.
export const TOPBAR_HEIGHT = 36; // px — matches TopBar's h-9
export const DOCK_CLEARANCE = 96; // px — matches Desktop's pb-24, reserves room for the dock
